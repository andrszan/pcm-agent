[project]
name = "pcm-demo"
version = "0.1.0"
description = "PCM 自动化流程前置验证工具"
requires-python = ">=3.11"
dependencies = [
    "claude-agent-sdk>=0.2.152",
    "openai",
    "pydantic-settings>=2.15,<3",
]
