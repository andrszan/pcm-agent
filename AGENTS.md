# Agent 工作规范

本仓库是 AI Agent 项目开发工作区：根目录管理项目立项、需求、架构设计、Backlog 等文档和协作记录；代码按严格的前后端分离原则放在 `frontend/` 和 `backend/`，并分别作为独立 Git 仓库管理。目录名是工作区约定，不要求与实际远程仓库名一致。

## 1. 先想再写

不要臆测，不要掩盖困惑。动手前：

- 明确假设；存在多种理解时列出取舍。
- 有更简单的方案时优先说明并采用简单方案。
- 需求、边界或成功标准不清楚时先提出问题，不要悄悄决定。
- 模型 prompt 的首行 slash command 可以显式选择当前能力；其余正文只描述当前领域功能、权威输入、输出和约束，不写“第几步”、PCM 阶段、节点、session 或 Skill 编排等外层背景；这些信息只保留在流程文档、状态和调度代码中。

## 2. 简单、精准地改动

- 只实现用户要求的内容，不增加投机性功能、抽象、配置或错误处理。
- 不顺手重构、格式化或修改未涉及的代码。
- 遵循现有风格；只清理因本次改动产生的孤儿 import、变量或函数。
- 每一行改动都应能直接追溯到用户请求。

## 3. 语言

面向用户和维护者的文档、说明、注释等默认使用中文。文件名、命令、配置键、代码标识符、框架名及其他通用技术名词可保留英文。除非用户明确要求，不要将说明改成英文。

## 4. 环境配置与敏感信息

- 在本项目及其独立子仓库范围内，允许 Agent 为开发、调试和验证读取、修改和使用开发环境 `.env`。
- 代码新增、删除或调整环境变量时，同步实际开发 `.env` 与 `.env.example`。`.env` 必须使用真实可用的值。`.env.example` 只保存配置键、可公开的开发默认值和安全占位说明，不复制密码、密钥或其他秘密。
- 可以补齐本机可生成或项目已提供的开发配置，但不得伪造外部账号、凭据、授权或不可替代资源。缺少这类条件时，应说明已尝试动作、真实缺口和解除条件。
- 除非任务明确要求，不在回复、日志、命令输出、提交或外部系统中展示 `.env` 的具体值；必要时只报告配置键和脱敏状态。
### 开发验收数据基线

- “开发验收数据基线”是目标项目受版本控制维护的一组产品内账号与角色、必要参考数据、代表性业务对象与状态、适用对象资源、恢复方式、验证方式和交付说明。其目标是让接手者在项目专用开发或验收环境中，从正常入口自主体验主要已交付功能；基线定义、恢复/核验实现和交付说明受版本控制，环境中的实际数据按这些内容建立和恢复。
- 它不等于 `project-readiness` 管理的外部服务、外部账号、运行凭据、私有数据或设备等开发资源准备基线，也不等于 `media-assets` 自带的测试 fixture；任意测试临时数据、生产数据、真实个人数据，以及目标产品访问其它系统或资源的凭据都不属于开发验收数据基线。
- 恢复方式按项目事实选择，可以是非破坏性的 initialize、独立 verify、重建专用环境后重新 Seed，或仅对管理范围明确的专用环境执行严格受控的 destructive reset。不得强制所有项目采用统一命令、技术栈、Manifest 或原地 reset；恢复必须幂等或具有等价的重复执行保护，不产生重复基线数据，也不覆盖管理范围外的已有账号、对象或资源。破坏性方式必须写明适用环境、管理范围、数据影响和安全前置条件。
- 项目 README 或既有受跟踪交付文档必须说明适用环境、前置服务、恢复与核验入口、基线管理范围、数据影响、账号文档位置、代表性业务对象及其资源范围和成功判断。若账号单独成文，应从 README 或既有交付文档可发现；创建、修改或删除基线内容时同步相关文档。
- 最终保留在基线中且交付后仍可登录的所有产品内开发账号及密码，均为受版本控制的产品交付信息，包括普通用户、业务角色、产品管理员和产品超级管理员。账号必须项目专用、不冒用真实个人身份、在基线恢复后真实存在且可从正常入口登录；真实开发数据不等于真实个人或生产数据。不得将这类账号称为演示账号、体验账号或假账号，也不得附加演示租户、隔离业务数据或生产禁用 Seed 等部署阶段限制。账号文档至少记录适用环境、登录入口、角色、账号、密码、恢复方式和必要已有数据说明；开发完成前逐个核验账号存在、密码可登录、角色一致且约定数据可见。
- 临时测试中创建后删除或事务回滚、未保留在最终交付开发状态中的短期账号和数据不进入基线，也无需逐项记录。
- 凭据边界按认证目标判断：用于登录目标产品的基线账号公开；PCM 或目标产品用于访问其它系统或资源的凭据仍属秘密，包括数据库、对象存储、SMTP、OAuth client secret、API key、私钥、令牌、Git、云、服务器、基础设施账号、PCM 工具和媒体 Provider 凭据及真实个人认证信息。此类秘密继续保存在受保护且 Git 忽略的配置中；`docs/ignore/` 只用于真正私密的开发资源资料，不承担目标产品账号或开发验收数据基线交接。

### 工作区媒体工具

- `media-assets` 是 AI Agent 工作区固定可用、按需调用的开发工具。涉及视觉媒体时，先依据项目事实判断其是否有明确价值，不为装饰而机械检索、下载或生成资源。
- 工作区媒体 Provider 的私有配置不属于目标产品模块依赖；不得写入目标产品配置、准备清单、业务数据、测试清单或客户端可见内容。
- 最终选定的静态媒体必须固化到目标项目约定的资产位置，不得将第三方临时 URL 作为产品依赖。不得通过本工具批量填充业务数据、创建对象存储或展开产品媒体迁移。

## 5. 仓库边界

工作区包含根仓库以及可能独立存在的多个代码仓库：

- `frontend/` 是前端项目的固定工作区，初始化 Git 后按独立仓库处理。
- `backend/` 是后端项目的固定工作区，初始化 Git 后按独立仓库处理。

  ```bash
  pwd
  git rev-parse --show-toplevel
  git status --short
  ```

- 除非任务明确要求使用 worktree，否则不要创建、进入或切换 worktree。
- 不要在根目录执行 `git add .`、`git clean -fdx` 或其他可能误纳入或删除 ignored 内容的等价命令。
- 执行清理、提交、分支或合并操作前，确认目标仓库和文件范围；汇报时说明涉及的仓库及各自验证结果。

## 6. 验证、状态与 Git 操作

- 把任务转化为可验证目标；修 Bug 先建立复现，改行为后运行相关测试或检查。
- 只验证受影响的仓库；因环境或依赖无法验证时，准确说明未验证范围和原因。
- 所有为开发验证、测试或 UI 浏览器验收生成，且不作为正式交付资产的截图，统一保存到工作区根目录的 `.test-screenshots/`。该目录由 Git 忽略，Agent 按需创建并自行组织内部子目录；不得将此类截图散落在工作区根目录或其它受跟踪位置。需要纳入版本控制的文档配图、产品资产和视觉回归基线不适用本规则，应放入项目约定的受跟踪目录。
- 禁止执行或要求屏幕阅读器(NVDA、VoiceOver)验证，也不得将其写入需求、TRD、验证计划、完成条件、缺口或交付说明。
- commit 使用 Conventional Commits：`<type>[optional scope]: <description>`。`type` 和可选 `scope` 使用英文；description、body、footer 默认使用中文。

例如：

```text
docs: 完善项目工作区和 Git 边界说明
```
