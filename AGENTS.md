# Agent 工作规范

## 1. 依据事实，合理决策

- 先读取项目说明、代码、配置、测试和 Git 状态，不用猜测填补事实缺口。
- 能依据已核验事实安全决定的普通事项自行处理；需求、边界或完成标准仍不清楚且会实质改变结果时，再向负责人确认。
- 请求决定时讲清问题、已知事实、约束、可行选项、主要影响和推荐理由，不能只提供文件路径或事项名称。

## 2. 只做必要改动

- 每项改动都服务当前请求，不增加投机性功能、抽象、配置或脚手架。
- 优先复用项目已有实现、标准库、平台能力和现有依赖。
- 遵循周边代码风格，只清理由本次修改产生的孤儿内容。

## 3. 沟通与文档

- 面向用户和维护者的说明、文档、注释默认使用中文；代码标识符、配置键、命令和通用技术名词可以保留英文。
- 文档解释机制本身，不用文件路径或术语堆砌代替说明。
- 报告真实结果：测试失败、环境缺失或验证未完成时明确说明，不把历史结果和代码阅读冒充本次验证。

## 4. 配置与凭据

- 密码、API key、token、私钥、个人认证信息和外部服务凭据不得进入 Git、日志、截图、回复或公开文档。
- 真实开发配置保存在项目约定且 Git 忽略的 `.env` 或等价受保护位置；`.env.example` 只包含键名、安全占位和明确无风险的本地默认值。
- 账号文档可以记录环境、登录入口、角色、账号标识、Seed/reset 方式和安全获取凭据的方法，但不记录真实密码。
- 验证时可以在授权范围内使用受保护配置，不得回显具体值；外部账号、授权或资源缺失时如实说明解除条件。

## 5. 本地运行与验证

- 按目标项目自己的 README、版本文件、依赖声明和运行配置使用正确命令，不凭经验替换技术栈。
- 只停止本次明确启动且归属可确认的进程，不清理其它项目或会话的资源。
- UI 或用户交互任务使用项目已有的可靠浏览器通道检查真实动态行为和代表性渲染结果；工具缺失时保留验证缺口。
- 媒体资源应先复用项目已有资产；确有价值且写入范围已获授权时，再使用适用工具检索、生成、检查和固化。

## 6. 仓库与 Git 边界

- 修改、提交、切换分支或合并前，确认目标仓库、当前分支、工作树状态和变更范围。
- 未明确要求时不创建 worktree，不暂存、不提交、不 push，也不改写 Git 历史。
- 不使用 `git add .`、`git clean -fdx` 或等价的宽泛操作；只处理归属明确的文件。
- 不覆盖、回滚或删除不属于当前任务的已有修改。
- commit 使用 Conventional Commits：`<type>[optional scope]: <description>`；type、scope 使用英文，description、body、footer 默认使用中文。

## 7. 完成与汇报

- 开始前把请求转化为可验证目标；修 Bug 时先定位根因和受影响调用方。
- 根据风险运行相关测试、静态检查、构建、服务、数据或浏览器验证，不机械执行无关命令。
- 分仓库说明改动和验证结果；无法执行的步骤写明原因、影响和补验方式。
- 只有代码、文档、测试和运行事实共同满足完成标准时，才报告任务完成。
