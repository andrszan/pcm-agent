# Agent 工作规范

本仓库是 AI Agent 项目开发工作区：根目录管理项目立项、需求、架构设计、Backlog 等文档和协作记录；代码按严格的前后端分离原则放在 `frontend/` 和 `backend/`，并分别作为独立 Git 仓库管理。目录名是工作区约定，不要求与实际远程仓库名一致。

## 1. 先想再写

不要臆测，不要掩盖困惑。动手前：

- 明确假设；存在多种理解时列出取舍。
- 有更简单的方案时优先说明并采用简单方案。
- 需求、边界或成功标准不清楚时先提出问题，不要悄悄决定。
- 设计或实现 PCM Demo 的新步骤前，先对照原手稿、当前 PCM 流程设计和活动 TRD，与开发者确认该步的输入、输出、前置条件、操作、完成条件及失败、阻塞和恢复边界。确认后先实现代码并通过测试和真实验证，再将实际实现同步到设计文档并提交；不得仅根据精简流程摘要自行补全操作语义。
- 模型 prompt 的首行 slash command 可以显式选择当前能力；其余正文只描述当前领域功能、权威输入、输出和约束，不写“第几步”、PCM 阶段、节点、session 或 Skill 编排等外层背景；这些信息只保留在流程文档、状态和调度代码中。

## 2. 简单、精准地改动

- 只实现用户要求的内容，不增加投机性功能、抽象、配置或错误处理。
- 不顺手重构、格式化或修改未涉及的代码。
- 遵循现有风格；只清理因本次改动产生的孤儿 import、变量或函数。
- 每一行改动都应能直接追溯到用户请求。

## 3. Skills 与职责边界

- 不要求所有项目使用固定的 Skill 或插件组合；根据当前项目和任务选择。
- 当已有 Skill 能明显提高质量时，先阅读对应 `SKILL.md` 并按其职责执行；简单任务不要为了使用 Skill 而使用。
- 每个 Skill 和 Subagent 只完成自身职责，不假设固定上下游，不自动调用另一个自定义 Skill，也不替流程中的其它步骤完成提交、合并或生命周期同步。
- 第三方 Skill 不是核心流程的隐藏依赖。除非用户明确要求或确认，不安装新的第三方 Skill、插件或 CLI。
- Git 提交仅由明确的提交步骤执行；需要提交时使用 `commit-changes`。普通领域 Skill、`dev-workflow`、`session-rule-retrospective` 和 Subagent 均留下待提交变更，不自行暂存或提交。

## 4. 语言

面向用户和维护者的文档、说明、注释等默认使用中文。文件名、命令、配置键、代码标识符、框架名及其他通用技术名词可保留英文。除非用户明确要求，不要将说明改成英文。

## 5. 环境配置与敏感信息

- 在本项目及其独立子仓库范围内，允许 Agent 为开发、调试和验证读取、修改和使用开发环境 `.env`；实际 `.env` 必须保持 Git 忽略。
- 代码新增、删除或调整环境变量时，同步实际开发 `.env` 与 `.env.example`。`.env.example` 只保存配置键、可公开的开发默认值和安全占位说明，不复制密码、密钥或其他秘密。
- 可以补齐本机可生成或项目已提供的开发配置，但不得伪造外部账号、凭据、授权或不可替代资源。缺少这类条件时，应说明已尝试动作、真实缺口和解除条件。
- 验证应使用项目实际支持的配置加载方式，不假设所有 `.env` 都能被 shell `source`；结构化值等特殊格式按项目事实安全加载。
- 除非任务明确要求，不在回复、日志、命令输出、提交或外部系统中展示 `.env` 的具体值；必要时只报告配置键和脱敏状态。

## 6. 仓库边界

工作区包含根仓库以及可能独立存在的多个代码仓库：

- `frontend/` 是前端项目的固定工作区，初始化 Git 后按独立仓库处理。
- `backend/` 是后端项目的固定工作区，初始化 Git 后按独立仓库处理。
- `pcm-demo/` 是当前能力仓库内的验证工具；它创建的产品项目位于配置的 `PCM_WORKSPACE_ROOT/<project_directory_name>/`，不属于当前能力仓库。对该目录执行模板克隆、清理或删除前，必须按活动 TRD 核验其属于当前 run，不能把当前能力仓库本身当作目标项目。
- 根仓库的状态干净，不代表任何子仓库干净；必须分别检查。
- 修改文件前，先确认文件所属仓库：

  ```bash
  pwd
  git rev-parse --show-toplevel
  git status --short
  ```

- 除非任务明确要求使用 worktree，否则不要创建、进入或切换 worktree。
- 不要在根目录执行 `git add .`、`git clean -fdx` 或其他可能误纳入或删除 ignored 内容的等价命令。
- 执行清理、提交、分支或合并操作前，确认目标仓库和文件范围；汇报时说明涉及的仓库及各自验证结果。

## 7. 验证、状态与 Git 操作

- 把任务转化为可验证目标；修 Bug 先建立复现，改行为后运行相关测试或检查。
- 只验证受影响的仓库；因环境或依赖无法验证时，准确说明未验证范围和原因。
- 普通实现步骤只维护与当前实现直接相关的活动文档和交付证据，不自行宣布整个需求完成，也不自动同步 Backlog、最终状态或完成日期。
- 需求状态、活动 TRD、Backlog 总览和详情卡的最终同步，由流程中的显式收尾步骤基于实际开发、验证和合并结果统一完成。
- 正式需求的建分支、合并回 `main` 和分支清理属于流程中的显式 Git 步骤，不由领域 Skill 或 Subagent 擅自执行。下一个需求应从最新 `main` 开始，不能沿用上一需求的特性分支。
- 当任务明确进入提交步骤时，按仓库分别精确提交，默认只 commit、不 push；不得用宽泛暂存命令混入无关修改。
- commit 使用 Conventional Commits：`<type>[optional scope]: <description>`。`type` 和可选 `scope` 使用英文；description、body、footer 默认使用中文。

例如：

```text
docs: 完善项目工作区和 Git 边界说明
```
