# Agent 工作规范

根工作区管理项目文档与协作记录；产品代码按前后端分离原则放在 `frontend/`、`backend/`，初始化 Git 后分别作为独立仓库管理。目录名是工作区约定，不要求与远程仓库同名。

## 1. 依据事实，合理决策

- 不臆测、不掩盖不确定性。先查已有资料和现场事实，说明必要假设；有更简单的可行方案时优先采用并说明。
- 能依据已核验事实安全决定的普通事项自行处理。需求、边界或完成标准仍不清楚，且需要负责人决定时，先问清再动手。
- 请求决定时，在同一回复中讲清问题、事实与约束、可行选项、影响和推荐理由；只有一个可行选项也要说明。引用只用于定位，不能代替解释。

## 2. 只做必要改动

- 每项改动都应服务当前请求，不添加投机性功能、抽象、配置或错误处理，不顺手重构或格式化无关内容。
- 遵循现有风格，只清理本次改动产生的孤儿 import、变量和函数。

## 3. 使用中文

面向用户和维护者的说明、文档、注释默认使用中文；文件名、命令、配置键、代码标识符和通用技术名词可保留英文。用户明确要求时例外。

## 4. 配置与凭据

- 允许为本项目及独立子仓库的开发、调试和验证读取、修改、使用开发 `.env`。
- 增删或调整环境变量时，同步实际 `.env` 与 `.env.example`。实际配置使用真实可用值；示例只放配置键、可公开的开发默认值和安全占位说明。
- 可补齐本机能生成或项目已提供的配置，不伪造外部账号、凭据、授权或资源。确有缺口时说明已尝试动作、缺少什么及解除条件。
- 最终保留且交付后仍可登录的目标产品开发账号及密码，随受跟踪的产品文档交付；访问其它系统或资源的凭据、工作区工具凭据及真实个人认证信息仍属秘密，存放在受保护且 Git 忽略的配置中。`docs/ignore/` 只放私密资源资料，不用于交接产品账号。
- 除非任务明确要求，不在回复、日志、命令输出、提交或外部系统中展示 `.env` 具体值；只报告配置键和脱敏状态。

## 5. 本地运行与媒体

- 存在 `.pcm/runtime.json` 时，启动、健康检查、联调、CORS 和浏览器验收使用其中分配的 host、port、endpoint；按真实技术栈接入配置或命令，不自行递增、随机换端口或允许框架静默换端口。未知进程占用时报告冲突。
- 只停止本次明确启动且归属可确认的进程；并发浏览器使用可关联产品和 run 的命名 session，不以全局关闭命令清理其它产品的浏览器。
- 设计或实现用户可见界面时，主动评估媒体对内容理解、场景、品牌和产品展示的价值；有收益且来源、成本、写入范围已获授权时，使用工作区 `media-assets` 获取、检查并固化素材，不等待用户点名，也不强行配图。媒体工具的私有配置不进入产品运行配置、资源准备清单或客户端可见内容。

## 6. 仓库与 Git 边界

- 清理、提交、分支或合并前，确认目标仓库、当前分支和变更范围；汇报时区分涉及的仓库。
- 未明确要求时不创建、进入或切换 worktree。
- 不在根目录执行 `git add .`、`git clean -fdx` 或等价的宽泛暂存、清理命令，避免误纳入或删除 ignored 内容。
- commit 使用 Conventional Commits：`<type>[optional scope]: <description>`；type、scope 用英文，description、body、footer 默认用中文。

## 7. 验证与汇报

- 把任务转化为可验证目标；修 Bug 先建立复现，改行为后运行相关测试或检查。
- 只验证受影响的仓库，分别报告结果；无法验证时说明范围和原因。
- 非交付用途的开发、测试及浏览器验收截图统一放在根工作区 Git 忽略的 `.test-screenshots/`，按需创建和组织子目录；受版本控制的文档配图、产品资产和视觉回归基线放在项目约定位置。
- 不执行或要求屏幕阅读器验证，也不将其列入需求、TRD、验证计划、完成条件、缺口或交付说明。
