# Agent 工作规范

本仓库是 AI Agent 项目开发工作区：根目录管理项目立项、需求、架构设计、Backlog 等文档和协作记录；代码按严格的前后端分离原则放在 `frontend/` 和 `backend/`，并分别作为独立 Git 仓库管理。目录名是工作区约定，不要求与实际远程仓库名一致。

## 1. 先想再写

不要臆测，不要掩盖困惑。动手前：

- 明确假设；存在多种理解时列出取舍。
- 有更简单的方案时优先说明并采用简单方案。
- 需求、边界或成功标准不清楚时先提出问题，不要悄悄决定。

## 2. 简单、精准地改动

- 只实现用户要求的内容，不增加投机性功能、抽象、配置或错误处理。
- 不顺手重构、格式化或修改未涉及的代码。
- 遵循现有风格；只清理因本次改动产生的孤儿 import、变量或函数。
- 每一行改动都应能直接追溯到用户请求。

## 3. Skills

- 不要求所有项目使用固定的 skill 或插件组合；根据当前项目和任务选择。
- 当任务涉及专门领域、复杂工具链，或已有 skill 能明显提高质量时，应优先检查并使用合适的 skill；简单任务不要为了使用 skill 而使用。
- 发现尚未安装但适合当前任务的第三方 skill 时，先说明其用途、来源和变更范围，再安装；使用 `npx skills add <owner/repo@skill> -g -y` 会自动将安装结果和版本信息记录在 `skills-lock.json`。
- 具体 skill 的使用方法以对应的 `SKILL.md` 为准；本文件只规定选择和安装边界。

## 4. 语言

面向用户和维护者的文档、说明、注释等默认使用中文。文件名、命令、配置键、代码标识符、框架名及其他通用技术名词可保留英文。除非用户明确要求，不要将说明改成英文。

## 5. 环境配置与敏感信息

- 在本项目及其独立子仓库范围内，允许 Agent 为开发、调试和验证直接读取、修改和使用 `.env` 文件及其中的开发环境配置，不因包含密钥而默认拒绝，也不得把补齐配置留给用户手工处理。
- 代码新增、删除或调整环境变量时，必须同步实际开发 `.env` 与 `.env.example`：实际 `.env` 必须包含当前运行所需的全部配置，必填项具有非占位、格式有效且与本机开发服务匹配的值；可选项应使用能够直接启动的安全默认值或明确省略，不能因示例空值导致配置校验失败。
- 完成交付前必须使用实际 `.env` 执行项目规定的安装、迁移、启动和就绪检查，证明配置可以直接使用；仅更新 `.env.example`、仅通过临时命令行注入变量或要求用户启动后再补值，不算完成。
- `.env.example` 只保存配置键、可公开的开发默认值和安全占位说明，不复制实际 `.env` 的密码、密钥或其他秘密；实际 `.env` 必须保持 Git 忽略。
- 除非任务明确要求，尽量不要在回复、日志、命令输出、commit 或外部系统中展示或传播 `.env` 的具体值；必要时只报告配置键和脱敏状态。

## 6. 仓库边界

工作区包含根仓库以及可能独立存在的多个代码仓库：

- `frontend/` 是前端项目的固定工作区，初始化 Git 后按独立仓库处理。
- `backend/` 是后端项目的固定工作区，初始化 Git 后按独立仓库处理。
- 根仓库的状态干净，不代表任何子仓库干净；必须分别检查。
- 修改文件前，先确认文件所属仓库：

  ```bash
  pwd
  git rev-parse --show-toplevel
  git status --short
  ```

- 除非任务明确要求使用 worktree，否则不要创建、进入或切换 worktree。
- 不要在根目录执行 `git add .`、`git clean -fdx` 或其他可能误纳入或删除 ignored 内容的等价命令。
- 执行清理、提交等操作前，确认操作针对的仓库和文件范围；汇报时说明涉及的仓库及各自的验证结果。

## 7. 验证、状态与提交

- 把任务转化为可验证目标；修 bug 先建立复现，改行为后运行相关测试或检查。
- 只验证受影响的仓库；因环境或依赖无法验证时明确说明。
- 后端 `.env` 中 `ALLOWED_ORIGINS` 和 `CORS_ORIGINS` 是 JSON 数组格式，不能用 `source .env` 加载到 shell——未加引号的 JSON 数组会被 shell 拆成非法值，导致 pydantic_settings 解析失败。运行后端测试或需要注入 `.env` 配置时，必须用 Python 读取 `.env` 键值并注入子进程环境，不要用 `source`。
- 运行 E2E 测试需要演示密码时，不能在命令行直接放置密码值（会被权限分类器拒绝）。应从后端代码常量 `app.demo_seed.DEMO_INITIAL_PASSWORD` 读取，通过 Python 注入子进程环境，不打印不落盘。
- 不可以在git提交内容中添加 `Co-Authored-By` 署名。
- 交付完成后必须同步需求状态：将活动 TRD 的需求状态和完成日期更新为实际结果，并同步 Backlog 总览及对应详情卡的状态；若需求未完成或验证受阻，必须保留准确状态和阻塞说明，不得只回填实施结果而遗漏状态。
- 提交前检查需求状态、TRD、Backlog 和代码提交是否一致；状态同步与本次需求交付属于同一收尾动作。
- 正式需求开发在新分支上进行；交付完成并验证后，必须将该分支合并回 `main`，再删除或保留特性分支由项目约定决定。下一个需求应从最新的 `main` 开新分支，不能在上一需求的特性分支上继续开发。
- 默认只 commit、不 push；合并回 `main` 是本地操作，不涉及远程。
- commit 使用 Conventional Commits：`<type>[optional scope]: <description>`。`type` 和可选 `scope` 使用英文；description、body、footer 默认使用中文。

例如：

```text
docs: 完善项目工作区和 Git 边界说明
```