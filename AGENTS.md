# Agent 工作规范

本仓库是 AI Agent 项目开发工作区：根目录管理项目立项、需求、架构设计、Backlog 等文档和协作记录；代码按严格的前后端分离原则放在 `frontend/` 和 `backend/`，并分别作为独立 Git 仓库管理。目录名是工作区约定，不要求与实际远程仓库名一致。

## 1. 先想再写

不要臆测，不要掩盖困惑。动手前：

- 明确假设；存在多种理解时列出取舍。
- 有更简单的方案时优先说明并采用简单方案。
- 需求、边界或成功标准不清楚时先提出问题，不要悄悄决定。
- 确实需要负责人、用户或调用方作出决定时，在同一回复中自包含地说明准确问题、已核验的当前事实与约束、当前约束下实质可行的选项、各选项主要影响，以及推荐与理由；只有一个实质可行选项时明确说明。文件路径、章节、提交、代码符号和行号只能作为辅助定位，不能代替必要事实，也不得要求对方自行读取引用后猜测问题、选项或影响。已能依据当前事实安全决定的普通事项由 Agent 自主处理，不机械上抛。
- 模型 prompt 的首行 slash command 可以显式选择当前能力；其余正文只描述当前领域功能、权威输入、输出和约束，不写“第几步”、PCM 阶段、节点、session 或 Skill 编排等外层背景；这些信息只保留在流程文档、状态和调度代码中。

## 2. 简单、精准地改动

- 只实现用户要求的内容，不增加投机性功能、抽象、配置或错误处理。
- 不顺手重构、格式化或修改未涉及的代码。
- 遵循现有风格；只清理因本次改动产生的孤儿 import、变量或函数。
- 每一行改动都应能直接追溯到用户请求。

## 3. 语言

面向用户和维护者的文档、说明、注释等默认使用中文。文件名、命令、配置键、代码标识符、框架名及其他通用技术名词可保留英文。除非用户明确要求，不要将说明改成英文。

## 4. 环境配置与敏感信息

- 在本项目及其独立子仓库范围内，允许 Agent 为开发、调试和验证读取、修改和使用开发环境 `.env`。
- 代码新增、删除或调整环境变量时，同步实际开发 `.env` 与 `.env.example`。`.env` 必须使用真实可用的值。`.env.example` 只保存配置键、可公开的开发默认值和安全占位说明，不复制密码、密钥或其他秘密。
- 可以补齐本机可生成或项目已提供的开发配置，但不得伪造外部账号、凭据、授权或不可替代资源。缺少这类条件时，应说明已尝试动作、真实缺口和解除条件。
- 除非任务明确要求，不在回复、日志、命令输出、提交或外部系统中展示 `.env` 的具体值；必要时只报告配置键和脱敏状态。
- PCM 或 Agent 为目标产品创建或调整、最终保留在交付开发数据中且交付后仍可登录的所有开发账号及其密码，均为受版本控制的产品交付信息，包括普通用户、业务角色、产品管理员和产品超级管理员。使用项目专用、不冒用真实个人身份、在开发数据中真实存在且可从正常入口登录的真实开发账号；真实开发数据不等于真实个人或生产数据。不得将这类账号称为演示账号、体验账号或假账号，也不得附加演示租户、隔离业务数据、生产禁用 Seed 等部署阶段限制。
- 在项目约定的受跟踪 README 或独立账号文档中记录每个交付开发账号的适用开发环境、登录入口、角色、账号、密码、Seed/reset 方式及必要已有开发数据说明。开发完成前逐个核验账号真实存在、密码可登录、角色一致且已有开发数据可见；创建、修改或删除账号时同步文档。Seed/reset 保持幂等、不重复且不覆盖其管理范围外的已有账号或数据。临时测试中创建后删除或事务回滚、未保留在最终交付开发状态中的短期账号无需逐个记录。
- 凭据边界按认证目标判断：用于登录目标产品的交付开发账号公开；PCM 或目标产品用于访问其它系统或资源的凭据仍属秘密，包括数据库、对象存储、SMTP、OAuth client secret、API key、私钥、令牌、Git、云、服务器、基础设施账号、PCM 工具和媒体 Provider 凭据及真实个人认证信息。此类秘密继续保存在受保护且 Git 忽略的配置中；`docs/ignore/` 只用于真正私密的开发资源资料，不再承担目标产品账号交接。

### 本地运行端口

- 产品工作区存在 `.pcm/runtime.json` 时，它是当前宿主机为该产品分配的本地 host、port 和 endpoint 事实；启动、健康检查、前后端联调、CORS 和浏览器验收应使用这些值。
- 可以按真实技术栈把这些值接入项目自身 `.env`、配置或启动命令，但不得自行递增、随机选择或依赖框架静默切换端口。已分配端口被未知进程占用时报告冲突，不终止未知进程。
- 临时服务结束时只停止由当前执行明确启动且归属可确认的进程；并发浏览器任务使用可关联当前产品和 run 的命名 session，不以全局关闭命令清理其他产品的浏览器。

### 工作区媒体工具

- `media-assets` 是工作区固定可用、按需调用的开发期媒体工具，统一负责已有资产复用、图库检索、定制生成、传入与生成结果检查、资产固化及测试 fixture。设计或实现用户可见界面时，主动评估媒体对内容识别、场景理解、品牌表达和产品展示的价值，不等待用户逐项点名；官网首屏、产品介绍、内容封面和引导插画等场景尤其应评估。普通表单、密集操作和纯数据界面没有明确收益时保持克制，不要求每页配图。
- 存在明确收益且来源、成本和写入范围已获授权时，自主选择并落实合适素材，不默认以渐变、通用图标、随机占位图或大块空白替代；已有素材合适就复用，通用摄影适合检索，品牌化或特定构图可直接生成，不要求先穷尽图库。测试 fixture 仅用于验证，不作为正式产品配图的默认替代。
- 获取前明确用途、目标容器与比例、主体位置、文案留白、风格色彩及禁止元素。生成 prompt 只补齐这些影响结果的缺口；信息已经充分时直接使用，不追求长度，不机械检索案例、补镜头参数或产出多个方向。只请求方向建议时不生成；已授权且生成目标明确时必须执行工作区固定脚本并取得明确文件，不能只写 prompt 或循环检查尚未生成的文件。
- 生成默认单张，检查实际图像后仅针对具体缺陷在已授权的有限预算内修正；不无界重抽。最终验收在真实页面中检查桌面和移动端的裁剪、主体、文字对比及视觉层级，不能以文件存在或接口成功代替。产品标题、按钮、品牌文字和需维护的数据默认由页面、字体或可编辑图形承载，不烘焙进背景图。
- 工作区媒体工具的私有配置不属于目标产品模块依赖；不得写入目标产品前后端运行配置、准备清单、业务数据、测试清单或客户端可见内容。配置缺失、工具不可用或授权不足时根据真实错误报告缺口，不擅自安装新的工具或静默切换服务。
- 最终选定的静态媒体必须固化到目标项目约定的资产位置，不得将第三方临时 URL 作为产品依赖。允许围绕明确开发需求，在授权的数量、成本和写入范围内有限批量获取、检查和固化素材，包括供开发数据及 Seed 引用的配图；遵守 Provider 许可、缓存和限流要求，不以限速替代用途与许可判断，不进行无明确用途的系统性采集、遍历式下载或素材库囤积。媒体工具不承担产品运行时媒体服务，不直接管理业务数据库或 Seed 逻辑，不创建对象存储或展开产品媒体迁移；产品开发负责已固化资产的业务关联。

## 5. 仓库边界

工作区包含根仓库以及可能独立存在的多个代码仓库：

- `frontend/` 是前端项目的固定工作区，初始化 Git 后按独立仓库处理。
- `backend/` 是后端项目的固定工作区，初始化 Git 后按独立仓库处理。

  ```bash
  pwd
  git rev-parse --show-toplevel
  git status --short
  ```

- 除非任务明确要求使用 worktree，否则不要创建、进入或切换 worktree。
- 不要在根目录执行 `git add .`、`git clean -fdx` 或其他可能误纳入或删除 ignored 内容的等价命令。
- 执行清理、提交、分支或合并操作前，确认目标仓库和文件范围；汇报时说明涉及的仓库及各自验证结果。

## 6. 验证、状态与 Git 操作

- 把任务转化为可验证目标；修 Bug 先建立复现，改行为后运行相关测试或检查。
- 只验证受影响的仓库；因环境或依赖无法验证时，准确说明未验证范围和原因。
- 所有为开发验证、测试或 UI 浏览器验收生成，且不作为正式交付资产的截图，统一保存到工作区根目录的 `.test-screenshots/`。该目录由 Git 忽略，Agent 按需创建并自行组织内部子目录；不得将此类截图散落在工作区根目录或其它受跟踪位置。需要纳入版本控制的文档配图、产品资产和视觉回归基线不适用本规则，应放入项目约定的受跟踪目录。
- 禁止执行或要求屏幕阅读器(NVDA、VoiceOver)验证，也不得将其写入需求、TRD、验证计划、完成条件、缺口或交付说明。
- commit 使用 Conventional Commits：`<type>[optional scope]: <description>`。`type` 和可选 `scope` 使用英文；description、body、footer 默认使用中文。

例如：

```text
docs: 完善项目工作区和 Git 边界说明
```
