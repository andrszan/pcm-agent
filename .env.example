# AI Agent 工作区的开发期媒体工具配置示例。
# 真实值只写入调用方授权的受保护配置，不得复制到目标产品前后端运行配置。

# 图库检索：Pixabay
PIXABAY_API_BASE_URL=https://pixabay.com/api/
PIXABAY_API_KEY=

# 定制生图：gpt-image CLI 原生读取的唯一一套连接配置。
# 兼容服务通常需要 /v1 前缀，不填写完整的 /images/generations 路径。
OPENAI_BASE_URL=http://localhost:8317/v1
OPENAI_API_KEY=
# 模型默认使用插件的 gpt-image-2；其它模型通过 --model 显式指定。
# CLI 不读取模型名环境变量，不另设 OPENAI_MODEL 或 MEDIA_IMAGE_*。
